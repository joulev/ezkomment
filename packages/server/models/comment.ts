export type IComment = {
    commentId: string;
    createdAt: any; // will be changed later to <Firebase time stamp>
    updatedAt: any; // will be changed later to <Firebase time stamp>
    commentContent: string;
};
