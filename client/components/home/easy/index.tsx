import { FC } from "react";

import EasyConfiguration from "./configuration";

const EasySections: FC = () => {
  return (
    <div className="border-y border-card bg-card h-[1234px]">
      <EasyConfiguration />
    </div>
  );
};

export default EasySections;
